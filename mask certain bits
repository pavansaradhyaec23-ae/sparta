#include <stdio.h>
#include <stdint.h>

uint32_t fu(uint8_t da)
{
    uint8_t result = da & ((1 << 1) | (1 << 2) | (1<3) | (1<4) );
    return result;
}

int main()
{
    uint32_t d = fu(0b01110101);

    printf("Result = %u\n", d);

    return 0;
}
