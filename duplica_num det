Find the duplicate number on a given integer array.

WTD: Inspect the provided array. Determine if there's any integer that appears more frequently than it should, signifying a duplicate.




#include <stdio.h>

int main() {
    int n;

    printf("Enter size: ");
    scanf("%d", &n);

    int arr[n];

    // Input
    for(int i = 0; i < n; i++) {
        printf("Enter array value at %d: ", i);
        scanf("%d", &arr[i]);
    }

    // Print array
    printf("Array elements are:\n");

    for(int i = 0; i < n; i++) {
        printf("%d ", arr[i]);
    }

    // Assume first element is max and min
   

    // Find max and min
    for(int i = 0; i < n; i++) {
        for(int k=i+1; k<n;k++){
            if(arr[i]==arr[k]){
                printf("the elemts repea ar: %d  ",arr[i]);
                
            }
        }
    }

        
        

       
    

   

    return 0;
}
